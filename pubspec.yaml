name: rifa_app
description: App de la Rifa Cadena de Oro 18K
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
